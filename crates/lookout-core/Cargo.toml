[package]
name = "lookout-core"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "Lookout core — local-first web instrument engine (scaffold)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
