# Predictable defaults

| Profile | Behavior |
| --- | --- |
| `fast` | Bounded local HTML search and fetch |
| `quality` | Explicitly enables richer extraction and cache expansion |
| `research` | Explicit multi-step work with page and budget limits |

Browser rendering and model synthesis are opt-in. Private addresses, oversized
responses, and blocked pages fail honestly.
