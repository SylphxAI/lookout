# Tool surface — Lookout

## Primary

`web_search`, `web_fetch`, `web_extract`

## Advanced

`web_cache`, `web_crawl`, `web_research`

Default path: search → fetch → extract. Multi-GB browser is permanent non-goal for default install.
